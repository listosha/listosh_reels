# Пиллар 1 «Глюкоза» — пакет для публикации

8 рилсов из проекта `video-edit` (Пиллар 1, тема — сахар/глюкоза). Сгенерены 19.06.2026.
Каждый ролик: опенер-хук → кружок-на-чёрном с плашками → инфографика «качели» → CTA + ключ ГЛЮКОЗА,
бренд фиолет `#7b5cff`, субтитры. Превью — вертикаль 1080×1920, портрет + хук.

## Логика наименований
Слаг: **`glukoza-g{N}-<тема>`** (N — номер ролика в серии).

| Файл | Что | Площадка |
|------|-----|----------|
| `videos/<slug>.mp4`     | IG-вариант (вшитый CTA «Напиши ГЛЮКОЗА → в личку») | Instagram Reels |
| `videos/<slug>-yt.mp4`  | YT-вариант (вшитый CTA «Ссылка в закрепе ⬇»)        | YouTube Shorts  |
| `thumbs/<slug>.png`     | превью (одно на ролик, для обеих площадок)          | IG + YT         |

**Почему два файла:** у IG и YT разный вшитый призыв (IG — кодовое слово в директ, YT — ссылка в
закреплённом комментарии). Публикатор раньше брал один файл на slug; добавлена поддержка `-yt`:
`publish_reel.py::publish_yt` грузит `videos/<slug>-yt.mp4`, если он есть, иначе базовый `<slug>.mp4`.
IG-сторона (`publish_ig`) всегда берёт базовый `<slug>.mp4`. Превью — общее.

## Соответствие роликов
| slug | тема | хук (что в превью) |
|------|------|--------------------|
| glukoza-g1-proval-sil       | Послеобеденный провал сил | Клонит в сон после обеда — это **сахар** |
| glukoza-g2-poryadok-edy     | Порядок еды               | Тот же обед — скачок сахара **−73%** |
| glukoza-g3-tyaga-ne-slabost | Тяга не слабость          | Тяга к сладкому — **не слабость** |
| glukoza-g4-vrednyy-zavtrak  | Вредный «полезный» завтрак| Самый вредный — **«полезный»** завтрак |
| glukoza-g5-sok-kola         | Сок не полезнее колы      | Фреш ≈ **кола** по сахару |
| glukoza-g6-oden-uglevod     | Одень углевод             | Не ешь углевод **голым** |
| glukoza-g7-10-minut         | 10 минут после еды        | **−25%** к скачку за 10 минут |
| glukoza-g8-menopauza        | Менопауза не приговор     | Похудеть после менопаузы — **реально** |

## Ссылка на гайд «Вес в защите»
`https://app.listoshenkov.ru/?section=gaid-ves-v-zaschite`
- **YouTube:** эта ссылка — в **закреплённом первом комментарии** (`yt_comment` уже подставлен).
  Закрепить вручную в Studio (API закреплять не умеет). В описании ссылку НЕ даём (запас — если YT
  придержит коммент, продублировать в описание).
- **Instagram:** ссылки НЕТ принципиально (не кликабельны + комплаенс) — только keyword→DM:
  зритель пишет `ГЛЮКОЗА` в комментах → авто-DM (BotHelp) с гайдом. В IG-подписи ссылки быть не должно.

## Как отправить
1. **Заготовки подписей** — в `pillar1-glukoza.draft.json` (slug, title, caption, tags, yt_comment),
   `status: "draft"`, без дат. Ссылка гайда уже вставлена в `yt_comment`. Подписи черновые — проверь.
2. Проставить `date`/`time`, сменить `status` на рабочий (как у остальных записей `schedule.json`),
   перенести записи в `schedule.json`.
4. `git pull` → `git add` → `commit` → `push` (воркфлоу публикует по слотам; перед пушем ВСЕГДА pull,
   воркфлоу пишет статусы обратно).

## Исходник / пересборка
Рецепт ролика лежит в `video-edit/work/pillar1/` (reels_config.json, скрипты, кэш cuts_all.json,
бролл-клипы, плашки) — при правке перерендер там, готовый файл заново скопировать сюда под тем же slug.
